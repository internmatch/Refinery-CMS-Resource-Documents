<ul>
  <% if Admin::ResourceCategoriesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_resource_categories_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_resource_category_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::ResourceCategoriesController.sortable? and ResourceCategory.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Resource Categories"),
                 admin_resource_categories_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Resource Categories"),
                 admin_resource_categories_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>